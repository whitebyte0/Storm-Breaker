# Domain for HTTPS (leave as "localhost" for HTTP-only mode)
DOMAIN=localhost

# Email for Let's Encrypt (required when DOMAIN is a real domain)
EMAIL=admin@example.com

# Ports (change if 80/443 are already in use)
HTTP_PORT=80
HTTPS_PORT=443
